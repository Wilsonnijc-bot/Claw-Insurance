#!/usr/bin/env bash

set -euo pipefail

SCRIPT_PATH="${BASH_SOURCE[0]}"
case "$SCRIPT_PATH" in
  */*) SCRIPT_DIR="${SCRIPT_PATH%/*}" ;;
  *) SCRIPT_DIR="." ;;
esac
ROOT_DIR="$(cd -- "$SCRIPT_DIR" && pwd -P)"

cd "$ROOT_DIR"
exec python3 -m nanobot docker-up "$@"
